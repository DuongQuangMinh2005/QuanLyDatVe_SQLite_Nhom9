<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.quanlydatve_sqlite">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.QuanLyDatVe_SQLite"
        tools:targetApi="31">
        <activity
            android:name=".activities.BeginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".activities.DangNhapActivity" />
        <activity android:name=".activities.DangKyActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.TraCuuChuyenBay" />
        <activity android:name=".users.DatVeActivity" />
        <activity android:name=".activities.DanhSachChuyenBayPhuHopActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".users.HoaDonBooking" />
        <activity android:name=".users.LichSuDatVeActivity" />
        <activity android:name=".admin.AdminQuanLyChuyenBay" />
        <activity android:name=".users.QuanLyChuyenBayYeuThich" />

        <activity android:name=".admin.EditChuyenBayActivity"
            android:label="Chỉnh sửa chuyến bay" />

        <activity
            android:name=".activities.EditUserActivity"
            android:label="Chỉnh sửa thông tin" />

        <activity android:name=".admin.AddChuyenBayActivity"
            android:label="Thêm chuến bay chuyến bay" />
    </application>

</manifest>